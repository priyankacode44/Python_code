let x=5;
let y=2;
let Result=x+y;
console.log('Result is :'+Result);

x += 5;       //x=x+5
let z=10;      //integer
let a='10';    //string
comsole.log(z==a);   //true   (checks value)
console.log(z===a);  //false   (checks type)

comsole.log(z!=a);   //false   (checks value)
console.log(z!==a);  //true   (checks type)

///Type of Operator////

let num=new Number (123); ////exlicitely define
console.log(num instanceof Number);

